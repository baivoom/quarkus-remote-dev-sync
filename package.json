{
  "name": "hooks",
  "version": "1.0.0",
  "description": "",
  "main": "logger.js",
  "scripts": {
    "build": "esbuild main.js --bundle --outfile=scripts/quarkus.js --platform=node --bundle --minify"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fast-glob": "^3.2.7"
  },
  "devDependencies": {
    "esbuild": "^0.12.17"
  }
}
